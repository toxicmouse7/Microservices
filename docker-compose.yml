services:
  envoygateway:
    build: Envoy/
    ports:
      - 9901:9901
      - 10000:10000
    volumes:
      - ./Envoy/envoy.yaml:/etc/envoy/envoy.yaml
  
  authapi:
    image: authapi
    build:
      context: .
      dockerfile: AuthAPI/Dockerfile
    ports:
      - 5001:80
      
  coffeeapi:
    image: coffeeapi
    build:
      context: .
      dockerfile: CoffeeAPI/Dockerfile
    ports:
      - 5002:80
